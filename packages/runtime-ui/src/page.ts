export function uatPage(): string {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>CMeng Schedule Control Center</title>
<style>
:root{color-scheme:dark;--bg:#0a0d12;--panel:#111720;--panel2:#161e29;--line:#263140;--text:#e8eef7;--muted:#94a3b8;--accent:#65a7ff;--good:#49d17d;--warn:#f0b94d;--bad:#ff6b6b;--radius:12px}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:14px}
button,input,textarea,select{font:inherit}.app{display:grid;grid-template-columns:280px minmax(0,1fr);min-height:100vh}.sidebar{border-right:1px solid var(--line);background:#0c1118;padding:18px 14px;position:sticky;top:0;height:100vh;overflow:auto}.brand{padding:0 8px 18px}.brand h1{font-size:19px;margin:0 0 5px}.brand p{color:var(--muted);margin:0;font-size:12px}.nav-section{margin:12px 0}.nav-title{color:#718096;font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;padding:8px}.nav-btn{display:flex;width:100%;align-items:center;gap:9px;padding:9px 10px;border:0;border-radius:8px;background:transparent;color:#c8d3e2;text-align:left;cursor:pointer}.nav-btn:hover,.nav-btn.active{background:#172131;color:white}.nav-num{width:23px;height:23px;border:1px solid #344257;border-radius:6px;display:grid;place-items:center;font-size:10px;color:#91a3ba;flex:0 0 auto}.main{min-width:0}.topbar{position:sticky;top:0;z-index:5;display:flex;gap:12px;align-items:center;justify-content:space-between;padding:14px 22px;background:rgba(10,13,18,.94);backdrop-filter:blur(12px);border-bottom:1px solid var(--line)}.project-picker{display:flex;align-items:center;gap:8px}.project-picker input{width:190px}.input,.project-picker input,textarea,select{border:1px solid #314055;background:#0e141d;color:var(--text);border-radius:8px;padding:9px 10px;outline:none}.input:focus,.project-picker input:focus,textarea:focus{border-color:var(--accent)}.btn{border:1px solid #34445a;background:#172231;color:#dce7f5;border-radius:8px;padding:8px 11px;cursor:pointer}.btn:hover{background:#203044}.btn.primary{background:#245ea7;border-color:#3274ca;color:white}.btn.good{background:#14552f;border-color:#277a49}.content{padding:22px;max-width:1500px;margin:0 auto}.page-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin-bottom:18px}.page-head h2{font-size:22px;margin:0 0 6px}.page-head p{margin:0;color:var(--muted)}.badge{display:inline-flex;align-items:center;border:1px solid #37475a;border-radius:999px;padding:4px 8px;font-size:11px;color:#b8c6d8}.badge.good{border-color:#266b45;color:#72e49f}.badge.warn{border-color:#765c2c;color:#f2c76f}.badge.bad{border-color:#7c3434;color:#ff8a8a}.grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:14px}.card{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:15px;min-width:0}.span-3{grid-column:span 3}.span-4{grid-column:span 4}.span-6{grid-column:span 6}.span-8{grid-column:span 8}.span-12{grid-column:span 12}.kpi-label{color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.08em}.kpi-value{font-size:25px;font-weight:650;margin-top:6px;overflow-wrap:anywhere}.kpi-sub{color:var(--muted);font-size:12px;margin-top:4px}.section-title{font-weight:650;margin:0 0 12px}.muted{color:var(--muted)}.row{display:flex;gap:9px;align-items:center;flex-wrap:wrap}.stack{display:flex;flex-direction:column;gap:10px}.divider{height:1px;background:var(--line);margin:12px 0}.upload{border:1px dashed #384a61;border-radius:10px;padding:12px}.upload label{display:block;font-size:12px;color:#b9c6d6;margin-bottom:7px}.upload input[type=file]{width:100%}.status-line{font-size:12px;color:var(--muted);margin-top:8px;white-space:pre-wrap}.error{border-color:#6f3030!important;background:#241313!important}.success{border-color:#285f40!important}.evidence-required{background:#211b10;border:1px solid #6d572b;border-radius:10px;padding:15px}.evidence-required h3{color:#f1c563;margin:0 0 8px}.missing{display:flex;flex-wrap:wrap;gap:7px}.missing span{border:1px solid #6d572b;border-radius:999px;padding:5px 8px;color:#e0bd73;font-size:12px}.json{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;line-height:1.5;white-space:pre-wrap;overflow:auto;max-height:620px;background:#090d13;border:1px solid #212c3b;border-radius:9px;padding:13px;color:#bcd0e8}.data-table{width:100%;border-collapse:collapse;font-size:12px}.data-table th,.data-table td{text-align:left;padding:8px;border-bottom:1px solid #25303d;vertical-align:top}.data-table th{color:#94a6bd;font-weight:600}.data-table td{overflow-wrap:anywhere}.chart{height:260px;width:100%;overflow:hidden}.chart svg{width:100%;height:100%}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.form-grid .full{grid-column:1/-1}textarea{width:100%;min-height:170px;resize:vertical;font-family:ui-monospace,monospace;font-size:12px}.tabs{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:14px}.tab{padding:7px 10px;border-radius:8px;border:1px solid #334258;background:#101722;color:#bdcada;cursor:pointer}.tab.active{background:#1e3047;color:#fff;border-color:#42658c}.toast{position:fixed;right:22px;bottom:22px;background:#172231;border:1px solid #3a4a60;border-radius:9px;padding:11px 13px;max-width:360px;box-shadow:0 14px 40px rgba(0,0,0,.35);z-index:20;display:none}.toast.show{display:block}.small{font-size:11px}@media(max-width:1000px){.app{grid-template-columns:1fr}.sidebar{position:relative;height:auto;border-right:0;border-bottom:1px solid var(--line)}.nav-section.modules{display:grid;grid-template-columns:repeat(2,minmax(0,1fr))}.span-3,.span-4,.span-6,.span-8{grid-column:span 12}.topbar{position:relative}.content{padding:15px}}
</style>
</head>
<body>
<div class="app">
  <aside class="sidebar">
    <div class="brand"><h1>CMeng</h1><p>Schedule & Project Controls UAT</p></div>
    <div class="nav-section">
      <div class="nav-title">Workspace</div>
      <button class="nav-btn active" data-view="overview"><span class="nav-num">•</span>Project Overview</button>
      <button class="nav-btn" data-view="evidence"><span class="nav-num">↑</span>Evidence Workspace</button>
      <button class="nav-btn" data-view="director"><span class="nav-num">D</span>Project Director</button>
      <button class="nav-btn" data-view="board"><span class="nav-num">B</span>Board Report</button>
      <button class="nav-btn" data-view="cert"><span class="nav-num">✓</span>Release Certificate</button>
    </div>
    <div class="nav-section modules">
      <div class="nav-title" style="grid-column:1/-1">Schedule Modules</div>
      <div id="moduleNav"></div>
    </div>
  </aside>
  <main class="main">
    <header class="topbar">
      <div class="project-picker"><span class="muted">Project</span><input id="projectId" value="UAT-001" /><button class="btn" id="refreshBtn">Refresh</button></div>
      <div class="row"><span class="badge" id="releaseBadge">Checking release…</span><span class="badge warn">UAT · session data</span></div>
    </header>
    <div class="content" id="content"></div>
  </main>
</div>
<div class="toast" id="toast"></div>
<script>
const state={modules:[],view:"overview",moduleKey:null,project:null,cert:null};
const $=s=>document.querySelector(s);
const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\\\"":"&quot;","'":"&#39;"}[m]));
const projectId=()=>$("#projectId").value.trim()||"UAT-001";
function toast(msg){const t=$("#toast");t.textContent=msg;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),3500)}
async function api(path,opts={}){const r=await fetch(path,opts);const text=await r.text();let body;try{body=JSON.parse(text)}catch{body={raw:text}}if(!r.ok){throw new Error(body.error||body.message||text||("HTTP "+r.status))}return body}
function fmt(v){if(v===null||v===undefined)return "—";if(typeof v==="number")return Number.isInteger(v)?v.toLocaleString():v.toLocaleString(undefined,{maximumFractionDigits:3});return String(v)}
function kpi(label,value,sub=""){return '<div class="card span-3"><div class="kpi-label">'+esc(label)+'</div><div class="kpi-value">'+esc(fmt(value))+'</div><div class="kpi-sub">'+esc(sub)+'</div></div>'}
function fieldTable(obj){if(!obj||typeof obj!=="object")return "";const rows=Object.entries(obj).filter(([,v])=>typeof v!=="object"||v===null).map(([k,v])=>'<tr><th>'+esc(k)+'</th><td>'+esc(fmt(v))+'</td></tr>').join("");return rows?'<table class="data-table">'+rows+'</table>':""}
function jsonBlock(data){return '<details><summary class="muted small" style="cursor:pointer;margin:9px 0">Raw governed output</summary><pre class="json">'+esc(JSON.stringify(data,null,2))+'</pre></details>'}
function lineChart(points,keys){if(!Array.isArray(points)||points.length<2)return "";const width=900,height=240,pad=30;const vals=[];for(const p of points){for(const k of keys){const v=p[k];if(typeof v==="number")vals.push(v)}}if(!vals.length)return "";const min=Math.min(...vals),max=Math.max(...vals);const x=i=>pad+(i/(points.length-1))*(width-pad*2);const y=v=>height-pad-((v-min)/(max-min||1))*(height-pad*2);const lines=keys.map((k,idx)=>{const d=points.map((p,i)=>typeof p[k]==="number"?(i?"L":"M")+x(i).toFixed(1)+" "+y(p[k]).toFixed(1):"").join(" ");return '<path d="'+d+'" fill="none" stroke="currentColor" opacity="'+(1-idx*.15)+'" stroke-width="'+(3-idx*.3)+'"/>'}).join("");return '<div class="card span-12"><div class="section-title">Curve</div><div class="chart"><svg viewBox="0 0 '+width+' '+height+'" preserveAspectRatio="none">'+lines+'</svg></div><div class="muted small">'+keys.map(esc).join(" · ")+'</div></div>'}
function renderProjection(moduleKey,p){const parts=['<div class="grid">'];if(p.projectId!==undefined)parts.push(kpi("Project",p.projectId));if(p.dataDateIso!==undefined)parts.push(kpi("Data Date",p.dataDateIso));if(p.complete!==undefined)parts.push(kpi("Complete",p.complete?"Yes":"No"));if(p.activityCount!==undefined)parts.push(kpi("Activities",p.activityCount));if(p.rowCount!==undefined)parts.push(kpi("Rows",p.rowCount));if(p.candidateRows!==undefined)parts.push(kpi("Candidates",p.candidateRows));if(p.coveragePercent!==undefined)parts.push(kpi("Coverage",p.coveragePercent===null?"Unknown":p.coveragePercent+"%"));if(p.independentForecastCompletionIso!==undefined)parts.push(kpi("Independent Forecast",p.independentForecastCompletionIso));if(p.progressBases){for(const [k,v] of Object.entries(p.progressBases)){parts.push(kpi(k,v?.valuePercent===null?"Missing":v?.valuePercent+"%",v?.state||""))}}if(p.float){parts.push(kpi("Critical",p.float.criticalCount));parts.push(kpi("Near Critical",p.float.nearCriticalCount));}if(p.lookAhead){parts.push(kpi("Look-Ahead Rows",p.lookAhead.rowCount));parts.push(kpi("Overdue",p.lookAhead.overdueCount));}parts.push('</div>');const points=p.points||p.curve?.points||p.series?.[0]?.points; if(Array.isArray(points)){const sample=points[0]||{};const keys=Object.keys(sample).filter(k=>k!=="dateIso"&&typeof sample[k]==="number").slice(0,4);parts.push('<div class="grid" style="margin-top:14px">'+lineChart(points,keys)+'</div>')}parts.push('<div class="card" style="margin-top:14px">'+fieldTable(p)+jsonBlock(p)+'</div>');return parts.join("")}
function header(title,desc,action=""){return '<div class="page-head"><div><h2>'+esc(title)+'</h2><p>'+esc(desc)+'</p></div>'+action+'</div>'}
async function loadModules(){const m=await api("/api/schedule/modules");state.modules=m.modules;$("#moduleNav").innerHTML=m.modules.map((x,i)=>'<button class="nav-btn" data-module="'+esc(x.key)+'"><span class="nav-num">'+(i+1)+'</span>'+esc(x.title)+'</button>').join("")}
async function refreshProject(){state.project=await api("/api/projects/"+encodeURIComponent(projectId()));if(state.view==="overview")render()}
async function loadCert(){state.cert=await api("/api/schedule/certification");$("#releaseBadge").textContent="Release "+(state.cert.release||"unknown").slice(0,8)+" · 22/22";$("#releaseBadge").className="badge good"}
function setNav(){document.querySelectorAll(".nav-btn").forEach(b=>b.classList.toggle("active",(b.dataset.view&&b.dataset.view===state.view)||(b.dataset.module&&state.view==="module"&&b.dataset.module===state.moduleKey)))}
async function moduleView(key){state.view="module";state.moduleKey=key;setNav();const mod=state.modules.find(x=>x.key===key);$("#content").innerHTML=header(mod?.title||key,"Governed Schedule module output from current project evidence.")+'<div class="card">Loading…</div>';try{const r=await api("/api/projects/"+encodeURIComponent(projectId())+"/schedule/modules/"+encodeURIComponent(key));if(r.status==="evidence_required"){$("#content").innerHTML=header(mod?.title||key,"This module is implemented, but the project does not yet contain every required governed input.")+'<div class="evidence-required"><h3>Evidence required</h3><p class="muted">Missing inputs are not treated as zero and no synthetic result is published.</p><div class="missing">'+r.missingEvidence.map(x=>'<span>'+esc(x)+'</span>').join("")+'</div></div>'+jsonBlock(r)}else{$("#content").innerHTML=header(mod?.title||key,"Calculated from the current governed project sources.")+renderProjection(key,r.projection)}}catch(e){$("#content").innerHTML=header(mod?.title||key,"Module execution error")+'<div class="card error">'+esc(e.message)+'</div>'}}
function overview(){const p=state.project||{};return header("Project Overview","One place to see whether the project has enough governed evidence to drive Schedule analysis.")+'<div class="grid">'+kpi("Schedule Revisions",p.scheduleRevisionCount||0)+kpi("Activities",p.currentActivityCount||0)+kpi("Resources",p.resourceCount||0)+kpi("BOQ Uploads",p.boqUploadCount||0)+kpi("Quantity Model",p.hasQuantityModel?"Loaded":"Missing")+kpi("Claims Register",p.hasDelayClaims?"Loaded":"Missing")+kpi("Contract",p.hasContract?"Loaded":"Missing")+kpi("Board Report",p.hasBoardReport?"Ready/Stored":"Not built")+'</div><div class="grid" style="margin-top:14px"><div class="card span-8"><h3 class="section-title">Schedule revisions</h3>'+((p.scheduleRevisions||[]).length?'<table class="data-table"><thead><tr><th>Seq</th><th>Revision</th><th>Data Date</th><th>Activities</th></tr></thead><tbody>'+p.scheduleRevisions.map(r=>'<tr><td>'+esc(r.sequence)+'</td><td>'+esc(r.label)+'</td><td>'+esc(r.dataDateIso)+'</td><td>'+esc(r.activityCount)+'</td></tr>').join("")+'</tbody></table>':'<div class="muted">No XER schedule uploaded yet.</div>')+'</div><div class="card span-4"><h3 class="section-title">Source status</h3>'+fieldTable({currentScheduleRevisionId:p.currentScheduleRevisionId,resourceAssignments:p.resourceAssignmentCount,BOQ:p.latestBoq?("Verified "+p.latestBoq.verifiedRows+" rows"):"Missing",quantities:p.hasQuantityModel?"Loaded":"Evidence required",claims:p.hasDelayClaims?"Loaded":"Evidence required",EOT:p.hasEotContext?"Loaded":"Evidence required"})+'</div></div>'}
function evidenceView(){return header("Evidence Workspace","Upload source documents and add governed registers used by the 22 Schedule modules.")+'<div class="grid"><div class="card span-6"><h3 class="section-title">Schedule revision · XER</h3><div class="form-grid"><input class="input" id="revLabel" placeholder="Revision label e.g. Update 05"><input class="input" id="revSeq" type="number" min="1" placeholder="Sequence"><input class="input full" id="scheduleFile" type="file" accept=".xer,text/plain"><button class="btn primary full" id="scheduleUploadBtn">Upload XER revision</button></div><div class="status-line" id="scheduleStatus"></div></div><div class="card span-6"><h3 class="section-title">BOQ · Excel or PDF</h3><div class="form-grid"><input class="input full" id="boqFile" type="file" accept=".xlsx,.xlsm,.pdf"><button class="btn primary full" id="boqUploadBtn">Upload BOQ</button></div><div class="status-line" id="boqStatus"></div></div><div class="card span-6"><h3 class="section-title">Contract · PDF or DOCX</h3><div class="form-grid"><input class="input full" id="contractFile" type="file" accept=".pdf,.docx"><button class="btn primary full" id="contractUploadBtn">Upload Contract</button></div><div class="status-line" id="contractStatus"></div></div><div class="card span-6"><h3 class="section-title">Progress evidence</h3><p class="muted small">Physical, contractor-reported and certified values remain separate.</p><textarea id="progressJson">{
  "physical":{"valuePercent":54.8,"sourceRefs":["uat:physical"]},
  "contractorReported":{"valuePercent":55.6,"sourceRefs":["uat:contractor"]},
  "certified":{"valuePercent":53.9,"sourceRefs":["uat:certified"]}
}</textarea><button class="btn" data-json-kind="progress-evidence" data-json-source="progressJson">Save progress evidence</button></div><div class="card span-6"><h3 class="section-title">Delay / Notices / Claims register</h3><textarea id="claimsJson" placeholder="Paste governed DelayClaimsModel JSON"></textarea><button class="btn" data-json-kind="delay-claims" data-json-source="claimsJson">Save claims register</button></div><div class="card span-6"><h3 class="section-title">EOT contract context</h3><textarea id="eotJson" placeholder='{"contractualCompletionIso":"2026-12-31",...}'></textarea><button class="btn" data-json-kind="eot-context" data-json-source="eotJson">Save EOT context</button></div><div class="card span-6"><h3 class="section-title">Quantity mapping & installed progress</h3><textarea id="quantityJson" placeholder="Paste CanonicalQuantityProgressModel JSON"></textarea><button class="btn" data-json-kind="quantity-progress" data-json-source="quantityJson">Save quantity model</button></div><div class="card span-6"><h3 class="section-title">Look-Ahead readiness evidence</h3><textarea id="readinessJson" placeholder='{"A200":{"permit":{"state":"ready","sourceRefs":["permit:P1"]}}}'></textarea><button class="btn" data-json-kind="readiness" data-json-source="readinessJson">Save readiness evidence</button></div><div class="card span-12"><h3 class="section-title">Commercial & control registers for Project Director</h3><p class="muted small">Currencies remain separate. Do not convert unless you provide a governed FX basis in a later reporting layer.</p><textarea id="directorJson">{
  "variations":[], "invoices":[], "retentions":[], "bonds":[],
  "claimCommercials":[], "hseIncidents":[], "ncrs":[], "rfis":[], "permits":[],
  "boardEvidence":null
}</textarea><button class="btn" data-json-kind="director-controls" data-json-source="directorJson">Save Director controls</button></div></div>'}
async function uploadFile(fileInput,path,headers,statusEl){const input=$(fileInput),file=input.files?.[0];if(!file){toast("Choose a file first.");return}const el=$(statusEl);el.textContent="Uploading…";try{const r=await api(path,{method:"POST",headers:{"content-type":file.type||"application/octet-stream","x-source-filename":file.name,...headers},body:new Uint8Array(await file.arrayBuffer())});el.textContent="PASS\n"+JSON.stringify(r,null,2);el.parentElement.classList.add("success");await refreshProject();toast("Source accepted and processed.")}catch(e){el.textContent="FAILED\n"+e.message;el.parentElement.classList.add("error")}}
async function saveJson(kind,source){const text=$("#"+source).value.trim();if(!text){toast("Enter JSON evidence first.");return}let obj;try{obj=JSON.parse(text)}catch{toast("Invalid JSON.");return}try{await api("/api/projects/"+encodeURIComponent(projectId())+"/evidence/"+kind,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(obj)});await refreshProject();toast(kind+" saved.")}catch(e){toast(e.message)}}
async function directorView(){let stored=null;try{stored=await api("/api/projects/"+encodeURIComponent(projectId())+"/director-position")}catch{}return header("Project Director","One governed position for schedule, claims, commercial exposure and delivery controls.",'<button class="btn primary" id="computeDirectorBtn">Compute from project evidence</button>')+(stored?renderProjection("director",stored):'<div class="evidence-required"><h3>No Director position yet</h3><p class="muted">Load two schedule revisions, contract, claims/EOT context and Director control registers, then compute.</p></div>')}
async function boardView(){let stored=null;try{stored=await api("/api/projects/"+encodeURIComponent(projectId())+"/board-report")}catch{}return header("Board Report","Evidence-backed publication. A report cannot become board-ready without manifest and evidence receipts.",'<button class="btn primary" id="finalizeBoardBtn">Finalize from current Director position</button>')+(stored?renderProjection("board",stored):'<div class="evidence-required"><h3>No board-ready report yet</h3><p class="muted">Compute the Project Director position first, then finalize with source manifest and evidence receipt IDs.</p></div>')}
function certView(){const c=state.cert||{};return header("Release Certificate","Live Schedule completion scope and non-negotiable data-governance invariants.")+'<div class="grid">'+kpi("Scope",c.scope)+kpi("Modules",c.moduleCount)+kpi("Release",(c.release||"").slice(0,12))+kpi("Status",c.moduleCount===22?"22 / 22":"Check")+'</div><div class="card" style="margin-top:14px"><h3 class="section-title">Invariants</h3>'+fieldTable(c.invariants)+jsonBlock(c)+'</div>'}
async function render(){setNav();const el=$("#content");if(state.view==="overview")el.innerHTML=overview();else if(state.view==="evidence")el.innerHTML=evidenceView();else if(state.view==="cert")el.innerHTML=certView();else if(state.view==="director")el.innerHTML=await directorView();else if(state.view==="board")el.innerHTML=await boardView();bindDynamic()}
function bindDynamic(){const s=$("#scheduleUploadBtn");if(s)s.onclick=()=>uploadFile("#scheduleFile","/api/projects/"+encodeURIComponent(projectId())+"/schedule/uploads",{"x-revision-label":$("#revLabel").value||"UAT Update","x-revision-sequence":$("#revSeq").value||String((state.project?.scheduleRevisionCount||0)+1)},"#scheduleStatus");const b=$("#boqUploadBtn");if(b)b.onclick=()=>uploadFile("#boqFile","/api/projects/"+encodeURIComponent(projectId())+"/boq/uploads",{},"#boqStatus");const c=$("#contractUploadBtn");if(c)c.onclick=()=>uploadFile("#contractFile","/api/projects/"+encodeURIComponent(projectId())+"/contract/uploads",{},"#contractStatus");document.querySelectorAll("[data-json-kind]").forEach(btn=>btn.onclick=()=>saveJson(btn.dataset.jsonKind,btn.dataset.jsonSource));const d=$("#computeDirectorBtn");if(d)d.onclick=async()=>{try{const r=await api("/api/projects/"+encodeURIComponent(projectId())+"/director-position/compute",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(state.project?.hasDirectorEvidence?{}:{"variations":[],"invoices":[],"retentions":[],"bonds":[],"claimCommercials":[],"hseIncidents":[],"ncrs":[],"rfis":[],"permits":[],"boardEvidence":null})});toast("Director position computed.");await refreshProject();state.view="director";render()}catch(e){toast(e.message)}};const f=$("#finalizeBoardBtn");if(f)f.onclick=async()=>{const manifest=prompt("Source manifest ID for board publication:");if(!manifest)return;const receipts=prompt("Evidence receipt IDs, comma separated:");if(!receipts)return;try{await api("/api/projects/"+encodeURIComponent(projectId())+"/board-report",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({sourceManifestId:manifest,evidenceReceiptIds:receipts.split(",").map(x=>x.trim()).filter(Boolean),finalizedAt:new Date().toISOString()})});toast("Board report finalized.");state.view="board";render()}catch(e){toast(e.message)}}}
document.addEventListener("click",e=>{const v=e.target.closest("[data-view]");if(v){state.view=v.dataset.view;state.moduleKey=null;render()}const m=e.target.closest("[data-module]");if(m)moduleView(m.dataset.module)});
$("#refreshBtn").onclick=()=>refreshProject().then(render).catch(e=>toast(e.message));$("#projectId").addEventListener("change",()=>refreshProject().then(render));
(async()=>{try{await Promise.all([loadModules(),loadCert(),refreshProject()]);render()}catch(e){$("#content").innerHTML='<div class="card error">'+esc(e.message)+'</div>'}})();
</script>
</body>
</html>`;
}
